# This file describes an application. You can have multiple applications
# in the same project.


# The name of this app. Must be unique within a project.
name: php

# The toolstack used to build the application.
toolstack: "php:symfony"

# The relationships of the application with services or other applications.
# The left-hand side is the name of the relationship as it will be exposed
# to the application in the PLATFORM_RELATIONSHIPS variable. The right-hand
# side is in the form `<service name>:<endpoint name>`.
relationships:
    database: "mysql:mysql"

# The configuration of app when it is exposed to the web.
web:
    # The public directory of the app, relative to its root.
    document_root: "/"
    # The front-controller script to send non-static requests to.
    passthru: "/index.php"

# The size of the persistent disk of the application (in MB).
disk: 2048

# The mounts that will be performed when the package is deployed.
mounts:
    "/public/cache": "shared:files/cache"
    "/public/config": "shared:files/config"
    "/public/download": "shared:files/download"
    "/public/log": "shared:files/log"
    "/public/img": "shared:files/img"
    "/public/mails": "shared:files/mails"
    "/public/modules": "shared:files/modules"
    "/public/themes/default-bootstrap": "shared:files/themes-default-bootstrap"
    "/public/translations": "shared:files/translations"
    "/public/upload": "shared:files/upload"

# The hooks executed at various points in the lifecycle of the application.
hooks:
    # We run build hooks before your application has been packaged.
    build: |
        cd public
        patch -p1 <platform_override_database_default_credentials.patch
    # We run deploy hook after your application has been deployed and started.
    deploy: |
        cd /app/public
        cp -r cache-init/* cache
        cp -r config-init/* config
        cp -r download-init/* download
        cp -r img-init/* img
        cp -r log-init/* log
        cp -r mails-init/* mails
        cp -r modules-init/* modules
        cp -r themes/default-bootstrap-init/* themes/default-bootstrap
        cp -r translations-init/* translations
        cp -r upload-init/* upload
